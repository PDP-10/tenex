BEGIN
INTEGER REC,BR, EOF;
EXTERNAL PROCEDURE DDT;
INTEGER CHAN;
LABEL X,STOP,READIN,N,FYLE;
STRING S;

INTEGER ARRAY B[1:128];

STRING DATA;
REC_0;
OUTSTR("File  *");
CHAN _ OPENFILE(NULL,"RC");
SETINPUT(CHAN,200,0,EOF);
N:
OUTSTR("Block   *");
S_INCHWL; IF S THEN REC_CVD(S) ELSE
X:	REC_REC+1;
USETI(CHAN,REC); 
READIN: ARRYIN(CHAN,B[1],128);
IF EOF THEN OUTSTR("EOF = " & CVOS(EOF) & "
");
STOP:
OUTSTR("
DDT!
");
DDT;
GO TO X;
END;




